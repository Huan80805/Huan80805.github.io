---
interface Props {
  eyebrow: string;
  title: string;
  body: string;
  href: string;
}

const { eyebrow, title, body, href } = Astro.props;
---

<a class="feature-card" href={href}>
  <p class="eyebrow">{eyebrow}</p>
  <h2>{title}</h2>
  <p>{body}</p>
</a>
